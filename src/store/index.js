import { createStore } from 'vuex'
import LeftSideDrawer from './Left-side-drawer';

export default createStore({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    LeftSideDrawer
  }
})
